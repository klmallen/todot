import { Cancellable } from "@lincode/promiselikes";
import { throttleTrailing } from "@lincode/utils";
const contextStack = [];
const getContext = () => contextStack[contextStack.length - 1];
const contextLazyMemo = (value) => {
    var _a;
    var _b, _c;
    const context = getContext();
    return ((_a = (_b = context.data)[_c = context.position++]) !== null && _a !== void 0 ? _a : (_b[_c] = value(context)));
};
const contextMemo = (value) => {
    var _a;
    var _b, _c;
    const context = getContext();
    return ((_a = (_b = context.data)[_c = context.position++]) !== null && _a !== void 0 ? _a : (_b[_c] = value));
};
const run = (handle, cleanup, data, effectBody) => {
    contextStack.push({ position: 0, data, handle });
    cleanup === null || cleanup === void 0 ? void 0 : cleanup();
    const nextCleanup = effectBody();
    contextStack.pop();
    return nextCleanup;
};
const diffAndRun = (handle, cleanup, data, getStates, statesOld, effectBody) => {
    contextStack.push({ position: 0, data, handle });
    const states = getStates.map((s) => (typeof s === "function" ? s() : s));
    const shouldRun = !statesOld || states.some((s, i) => s !== statesOld[i]);
    shouldRun && (cleanup === null || cleanup === void 0 ? void 0 : cleanup());
    const nextCleanup = shouldRun ? effectBody() : undefined;
    contextStack.pop();
    return [nextCleanup, states, shouldRun];
};
export const createNestedEffect = (effectBody, getStates) => {
    const handle = contextLazyMemo((context) => context.handle.watch(new Cancellable(() => { var _a; return (_a = cleanupRef.current) === null || _a === void 0 ? void 0 : _a.call(cleanupRef); })));
    if (handle.done)
        return handle;
    const data = contextMemo({});
    const cleanupRef = createRef();
    const statesOldRef = createRef();
    const cleanup = cleanupRef.current;
    const statesOld = statesOldRef.current;
    const [nextCleanup, states, shouldRun] = diffAndRun(handle, cleanup, data, getStates, statesOld, effectBody);
    if (shouldRun)
        cleanupRef.current = nextCleanup;
    statesOldRef.current = states;
    return handle;
};
export const createEffect = (effectBody, getStates) => {
    const handle = new Cancellable(() => setTimeout(() => cleanup === null || cleanup === void 0 ? void 0 : cleanup()));
    const data = {};
    let cleanup;
    const rerun = throttleTrailing(() => {
        if (handle.done)
            return;
        cleanup = run(handle, cleanup, data, effectBody);
    });
    rerun();
    for (const getState of getStates)
        getState && handle.watch(getState(rerun));
    return handle;
};
export const createRef = (value) => contextLazyMemo(() => ({
    current: typeof value === "function" ? value() : value
}));
export const createPrevious = (value) => {
    const valueRef = createRef(value);
    createNestedEffect(() => {
        queueMicrotask(() => (valueRef.current = value));
    }, [value]);
    return valueRef.current;
};
export const createMemo = (value, getStates) => {
    const valueRef = createRef(value);
    const firstRef = createRef(true);
    createNestedEffect(() => {
        if (firstRef.current) {
            firstRef.current = false;
            return;
        }
        valueRef.current = value();
    }, getStates);
    return valueRef.current;
};
