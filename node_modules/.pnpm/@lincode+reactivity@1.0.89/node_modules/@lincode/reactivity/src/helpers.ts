import {
    omit as omit0,
    filter as filter0,
    valueof,
    traverse
} from "@lincode/utils"
import { SetGlobalState, GetGlobalState } from "./store"

export const assign =
    <T extends Record<any, any> | undefined>(
        setState: SetGlobalState<T>,
        getState: GetGlobalState<T>
    ) =>
    (val: Partial<T>) => {
        const state = getState()
        if (
            state &&
            Object.entries(val).some(([key, value]) => state[key] !== value)
        )
            setState({ ...state, ...val })
    }

const isObject = (obj: any) => obj && typeof obj === "object"
const isMergeable = (obj0: any, obj1: any) => {
    if (Array.isArray(obj0) && Array.isArray(obj1)) return true
    if (isObject(obj0) && isObject(obj1)) return true
    return false
}
const merge0 = <T extends object>(obj0: T, obj1: object): T => {
    if (Array.isArray(obj0) && Array.isArray(obj1)) {
        for (let i = 0; i < obj1.length; ++i)
            if (i < obj0.length) {
                if (isMergeable(obj0[i], obj1[i])) merge0(obj0[i], obj1[i])
                else obj0[i] = obj1[i]
            } else obj0.push(obj1[i])
        return obj0
    }
    for (const [key, value] of Object.entries(obj1)) {
        //@ts-ignore
        if (isMergeable(obj0[key], value)) merge0(obj0[key], value)
        //@ts-ignore
        else obj0[key] = value
    }
    return obj0
}

export const merge =
    <T extends Record<any, any> | undefined>(
        setState: SetGlobalState<T>,
        getState: GetGlobalState<T>
    ) =>
    (val: Partial<T>) => {
        const state = getState()
        state && setState({ ...merge0(state, val) })
    }

export const omit =
    <T extends Record<any, any> | undefined>(
        setState: SetGlobalState<T>,
        getState: GetGlobalState<T>
    ) =>
    (val: keyof T | Array<keyof T>) => {
        const state = getState()
        //@ts-ignore
        state && setState(omit0(state, val))
    }

type Filter = {
    <T extends Record<any, any> | undefined>(
        setState: SetGlobalState<T>,
        getState: GetGlobalState<T>
    ): (predicate: (v: valueof<T>, k: keyof T) => boolean) => void
    <T>(
        setState: SetGlobalState<Array<T>>,
        getState: GetGlobalState<Array<T>>
    ): (predicate: (v: T, i: number) => boolean) => void
}

export const filter: Filter =
    (setState: SetGlobalState<any>, getState: GetGlobalState<any>) =>
    (predicate: (v: any, k: any) => boolean) => {
        const state = getState()
        if (!state) return

        if (Array.isArray(state)) setState(state.filter(predicate))
        else setState(filter0(state, predicate))
    }

export const rename =
    <T extends Record<any, any> | Array<string> | string | undefined>(
        setState: SetGlobalState<T>,
        getState: GetGlobalState<T>
    ) =>
    (from: string, to: string) => {
        const state = getState()
        if (!state) return

        if (typeof state === "string") {
            //@ts-ignore
            state === from && setState(to)
            return
        }

        if (Array.isArray(state)) {
            let update = false
            const newState = state.map((val) => {
                if (val === from) {
                    update = true
                    return to
                }
                return val
            })
            //@ts-ignore
            update && setState(newState)
            return
        }

        //@ts-ignore
        if (!(from in state)) return

        //@ts-ignore
        setState(Object.assign(omit0(state, from), { [to]: state[from] }))
    }

export const renameStartsWith =
    <T extends Record<any, any> | Array<string> | string | undefined>(
        setState: SetGlobalState<T>,
        getState: GetGlobalState<T>
    ) =>
    (from: string, to: string) => {
        const state = getState()
        if (!state) return

        if (typeof state === "string") {
            //@ts-ignore
            state.startsWith(from) && setState(state.replace(from, to))
            return
        }

        if (Array.isArray(state)) {
            let update = false
            const newState = state.map((val) => {
                if (val.startsWith(from)) {
                    update = true
                    return val.replace(from, to)
                }
                return val
            })
            //@ts-ignore
            update && setState(newState)
            return
        }

        const replaced: Record<string, valueof<T>> = {}
        const filtered = filter0(state, (v, k) => {
            //@ts-ignore
            const replace = k.startsWith(from)
            //@ts-ignore
            replace && (replaced[k.replace(from, to)] = v)
            return !replace
        })

        if (Object.keys(replaced).length === 0) return

        //@ts-ignore
        setState(Object.assign(filtered, replaced))
    }

export const increase =
    (setState: SetGlobalState<number>, getState: GetGlobalState<number>) =>
    () =>
        setState(getState() + 1)

export const decrease =
    (setState: SetGlobalState<number>, getState: GetGlobalState<number>) =>
    () =>
        setState(getState() - 1)

type SetPointer<T> = Array<Set<T>> | [Set<T>]

export const add =
    <A>(
        setState: SetGlobalState<SetPointer<A>>,
        getState: GetGlobalState<SetPointer<A>>
    ) =>
    (val: A) => {
        const [set] = getState()
        if (set.has(val)) return
        set.add(val)
        setState([set])
    }

export const remove =
    <A>(
        setState: SetGlobalState<SetPointer<A>>,
        getState: GetGlobalState<SetPointer<A>>
    ) =>
    (val: A) => {
        const [set] = getState()
        if (!set.has(val)) return
        set.delete(val)
        setState([set])
    }

export const clear =
    <A>(
        setState: SetGlobalState<SetPointer<A>>,
        getState: GetGlobalState<SetPointer<A>>
    ) =>
    () => {
        const [set] = getState()
        if (!set.size) return
        set.clear()
        setState([set])
    }

export const push =
    <A>(
        setState: SetGlobalState<Array<A>>,
        getState: GetGlobalState<Array<A> | undefined>
    ) =>
    (val: A) => {
        const array = getState()
        array && setState([...array, val])
    }

export const pull =
    <A>(
        setState: SetGlobalState<Array<A>>,
        getState: GetGlobalState<Array<A> | undefined>
    ) =>
    (val: A) => {
        const array = getState()
        array && setState(array.filter((item) => item !== val))
    }

export const concat =
    <A>(
        setState: SetGlobalState<Array<A>>,
        getState: GetGlobalState<Array<A> | undefined>
    ) =>
    (val: Array<A>) => {
        const array = getState()
        array && setState([...array, ...val])
    }

export const refresh =
    <T extends Array<any> | Record<any, any> | undefined>(
        setState: SetGlobalState<T>,
        getState: GetGlobalState<T>
    ) =>
    () => {
        const state = getState()
        if (!state) return

        if (Array.isArray(state))
            //@ts-ignore
            setState([...state])
        else setState({ ...state })
    }

export const reset = <T extends Array<any> | Record<any, any> | undefined>(
    setState: SetGlobalState<T>,
    getState: GetGlobalState<T>
) => {
    const state = getState()

    if (state && typeof state === "object") {
        traverse(state, (_, v) => Object.freeze(v))
        Object.freeze(state)
    }
    return () => setState(state)
}
